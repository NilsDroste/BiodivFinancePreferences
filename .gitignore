# Raw survey microdata — GDPR, not deposited
data/

# Anonymized deposit data — tracked for public release (pid and q17 removed)
# deposit/ is intentionally NOT ignored

# Open-ended responses — excluded from deposit (re-identification risk)
analysis/q17_for_coding.csv

# Fitted model objects (large, reproducible from scripts)
analysis/*.rds
analysis/CL_full_iterations.csv
analysis/LCM_2class_iterations.csv
analysis/LCM_3class_iterations.csv
analysis/LCM_4class_iterations.csv
.vscode/

# HB approach not reported in paper
analysis/hb_wtp_feasibility.R
analysis/hb_wtp_stage2.rds

# Pilot analysis (pilot data not deposited)
analysis/pilot_analysis.R

# Internal helper scripts not needed for replication
# NOTE: _mxl_runner.R is NOT ignored. mxl_analysis.R invokes it as a subprocess,
# so without it the reported mixed logit cannot be reproduced from source.
analysis/_table.R

# Old manuscript versions and internal notes
paper/manuscript.qmd
paper/manuscript_EE.qmd
paper/manuscript_GEC_anon.qmd
paper/manuscript.fdb_latexmk
feedback/
claude/
design/Survey design.docx
# Email correspondence with the survey provider: contains names, phone numbers
# and personal email addresses of the provider and the research team. Retained
# locally as the record of the consent gate and field dates; never published.
design/ParticipantInfo.pdf


# Generated outputs
paper/*.pdf
paper/*.tex
paper/*.png
paper/*.fdb_latexmk
paper/*.rmarkdown
paper/manuscript_GEC_files/
paper/manuscript_files/
Rplots.pdf

# Internal project files
.claude/
"Guide for authors*.pdf"
analysis/analysis.qmd
paper/manuscript.html
paper/manuscript_GEC_anon_files/
paper/notes.md

# Quarto build cache
.quarto/

# renv: track lockfile and activate script; exclude the package library
renv/library/
renv/python/
renv/staging/

# OS / editor
.DS_Store
.Rhistory
.RData
